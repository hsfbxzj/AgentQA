diff --git a/config.example.yaml b/config.example.yaml
index 24b65bb..2cc635e 100644
--- a/config.example.yaml
+++ b/config.example.yaml
@@ -494,6 +494,7 @@ tool_groups:
   - name: file:read
   - name: file:write
   - name: bash
+  - name: agentqa
 
 # ============================================================================
 # Tools Configuration
@@ -501,6 +502,18 @@ tool_groups:
 # Configure available tools for the agent to use
 
 tools:
+  # AgentQA tools: create a reviewable OpenAPI test plan, then execute it only
+  # after explicit user approval. The network allowlist is local-only.
+  - name: agentqa_create_test_plan
+    group: agentqa
+    use: agentqa.tools:agentqa_create_test_plan_tool
+  - name: agentqa_execute_test_plan
+    group: agentqa
+    use: agentqa.tools:agentqa_execute_test_plan_tool
+  - name: agentqa_compare_test_reports
+    group: agentqa
+    use: agentqa.tools:agentqa_compare_test_reports_tool
+
   # Web search tool (uses DuckDuckGo, no API key required)
   - name: web_search
     group: web
diff --git a/docker/docker-compose-dev.yaml b/docker/docker-compose-dev.yaml
index 97cbbee..0aaf3b7 100644
--- a/docker/docker-compose-dev.yaml
+++ b/docker/docker-compose-dev.yaml
@@ -113,6 +113,36 @@ services:
       - deer-flow-dev
     restart: unless-stopped
 
+  # AgentQA Demo API - deterministic local test target with three seeded defects
+  agentqa-demo-api:
+    build:
+      context: ../
+      dockerfile: backend/Dockerfile
+      target: dev
+      args:
+        APT_MIRROR: ${APT_MIRROR:-}
+        UV_IMAGE: ${UV_IMAGE:-ghcr.io/astral-sh/uv:0.7.20}
+        UV_INDEX_URL: ${UV_INDEX_URL:-https://pypi.org/simple}
+    container_name: agentqa-demo-api
+    command:
+      - sh
+      - -c
+      - cd /app/backend && PYTHONPATH=. uv run --no-sync uvicorn agentqa_demo.app:app --host 0.0.0.0 --port 8000
+    volumes:
+      - ../backend/agentqa_demo:/app/backend/agentqa_demo:ro
+    working_dir: /app/backend
+    ports:
+      - "8003:8000"
+    networks:
+      - deer-flow-dev
+    restart: unless-stopped
+    healthcheck:
+      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
+      interval: 10s
+      timeout: 5s
+      retries: 6
+      start_period: 10s
+
   # Backend - Gateway API
   gateway:
     build:
@@ -169,8 +199,8 @@ services:
       - DEER_FLOW_SANDBOX_HOST=host.docker.internal
       # Proxy values (HTTP_PROXY/HTTPS_PROXY/ALL_PROXY) are inherited from ../.env via env_file.
       # Only NO_PROXY is declared here so internal service hostnames are always exempt from the proxy.
-      - NO_PROXY=${NO_PROXY:-}${NO_PROXY:+,}localhost,127.0.0.1,::1,gateway,frontend,nginx,provisioner,host.docker.internal
-      - no_proxy=${no_proxy:-}${no_proxy:+,}localhost,127.0.0.1,::1,gateway,frontend,nginx,provisioner,host.docker.internal
+      - NO_PROXY=${NO_PROXY:-}${NO_PROXY:+,}localhost,127.0.0.1,::1,gateway,frontend,nginx,provisioner,agentqa-demo-api,host.docker.internal
+      - no_proxy=${no_proxy:-}${no_proxy:+,}localhost,127.0.0.1,::1,gateway,frontend,nginx,provisioner,agentqa-demo-api,host.docker.internal
     env_file:
       - ../.env
     extra_hosts:
diff --git a/frontend/src/components/workspace/messages/message-list.tsx b/frontend/src/components/workspace/messages/message-list.tsx
index 8134154..68c8c6a 100644
--- a/frontend/src/components/workspace/messages/message-list.tsx
+++ b/frontend/src/components/workspace/messages/message-list.tsx
@@ -14,6 +14,7 @@ import {
   type TokenUsageInlineMode,
 } from "@/core/messages/usage-model";
 import {
+  extractAgentQAPlanFromMessage,
   extractContentFromMessage,
   extractPresentFilesFromMessage,
   extractTextFromMessage,
@@ -40,6 +41,7 @@ import { ArtifactFileList } from "../artifacts/artifact-file-list";
 import { CopyButton } from "../copy-button";
 import { StreamingIndicator } from "../streaming-indicator";
 
+import { AgentQAPlanCard } from "./agentqa-plan-card";
 import { MarkdownContent } from "./markdown-content";
 import { MessageGroup } from "./message-group";
 import { MessageListItem } from "./message-list-item";
@@ -316,6 +318,15 @@ export function MessageList({
                   )}
               </div>
             );
+          } else if (group.type === "assistant:agentqa-plan") {
+            const plan = group.messages
+              .map(extractAgentQAPlanFromMessage)
+              .find((candidate) => candidate !== null);
+            return plan ? (
+              <div key={group.id} className="w-full">
+                <AgentQAPlanCard plan={plan} />
+              </div>
+            ) : null;
           } else if (group.type === "assistant:clarification") {
             const message = group.messages[0];
             if (message && hasContent(message)) {
@@ -338,7 +349,10 @@ export function MessageList({
             const files: string[] = [];
             for (const message of group.messages) {
               if (hasPresentFiles(message)) {
-                const presentFiles = extractPresentFilesFromMessage(message);
+                const presentFiles = extractPresentFilesFromMessage(
+                  message,
+                  group.messages,
+                );
                 files.push(...presentFiles);
               }
             }
diff --git a/frontend/src/core/messages/utils.ts b/frontend/src/core/messages/utils.ts
index 3ced9d3..5ebe729 100644
--- a/frontend/src/core/messages/utils.ts
+++ b/frontend/src/core/messages/utils.ts
@@ -14,6 +14,8 @@ interface AssistantMessageGroup extends GenericMessageGroup<"assistant"> {}
 
 interface AssistantPresentFilesGroup extends GenericMessageGroup<"assistant:present-files"> {}
 
+interface AssistantAgentQAPlanGroup extends GenericMessageGroup<"assistant:agentqa-plan"> {}
+
 interface AssistantClarificationGroup extends GenericMessageGroup<"assistant:clarification"> {}
 
 interface AssistantSubagentGroup extends GenericMessageGroup<"assistant:subagent"> {}
@@ -23,6 +25,7 @@ export type MessageGroup =
   | AssistantProcessingGroup
   | AssistantMessageGroup
   | AssistantPresentFilesGroup
+  | AssistantAgentQAPlanGroup
   | AssistantClarificationGroup
   | AssistantSubagentGroup;
 
@@ -48,6 +51,7 @@ export function getMessageGroups(messages: Message[]): MessageGroup[] {
       last &&
       last.type !== "human" &&
       last.type !== "assistant" &&
+      last.type !== "assistant:agentqa-plan" &&
       last.type !== "assistant:clarification"
     ) {
       return last;
@@ -66,7 +70,16 @@ export function getMessageGroups(messages: Message[]): MessageGroup[] {
     }
 
     if (message.type === "tool") {
-      if (isClarificationToolMessage(message)) {
+      if (isAgentQAPlanToolMessage(message)) {
+        // Keep the tool result with its processing step, then also display the
+        // deterministic test plan as a standalone, always-visible card.
+        lastOpenGroup()?.messages.push(message);
+        groups.push({
+          id: message.id,
+          type: "assistant:agentqa-plan",
+          messages: [message],
+        });
+      } else if (isClarificationToolMessage(message)) {
         // Add to the preceding processing group to preserve tool-call association,
         // then also open a standalone clarification group for prominent display.
         lastOpenGroup()?.messages.push(message);
@@ -432,7 +445,13 @@ export function hasToolCalls(message: Message) {
 export function hasPresentFiles(message: Message) {
   return (
     message.type === "ai" &&
-    message.tool_calls?.some((toolCall) => toolCall.name === "present_files")
+    message.tool_calls?.some((toolCall) =>
+      [
+        "present_files",
+        "agentqa_execute_test_plan",
+        "agentqa_compare_test_reports",
+      ].includes(toolCall.name),
+    )
   );
 }
 
@@ -440,7 +459,78 @@ export function isClarificationToolMessage(message: Message) {
   return message.type === "tool" && message.name === "ask_clarification";
 }
 
-export function extractPresentFilesFromMessage(message: Message) {
+export interface AgentQATestPlanCase {
+  id: string;
+  name: string;
+  method: string;
+  path_template: string;
+  expected_status_codes: number[];
+  rule: string;
+}
+
+export interface AgentQATestPlan {
+  id: string;
+  api_title: string;
+  base_url: string;
+  cases: AgentQATestPlanCase[];
+}
+
+export function isAgentQAPlanToolMessage(message: Message) {
+  return message.type === "tool" && message.name === "agentqa_create_test_plan";
+}
+
+export function extractAgentQAPlanFromMessage(
+  message: Message,
+): AgentQATestPlan | null {
+  if (!isAgentQAPlanToolMessage(message)) {
+    return null;
+  }
+
+  try {
+    const payload = JSON.parse(extractTextFromMessage(message)) as {
+      plan?: unknown;
+    };
+    const plan = payload.plan as Record<string, unknown> | undefined;
+    if (
+      !plan ||
+      typeof plan.id !== "string" ||
+      typeof plan.api_title !== "string" ||
+      typeof plan.base_url !== "string" ||
+      !Array.isArray(plan.cases)
+    ) {
+      return null;
+    }
+
+    const cases = plan.cases.filter(
+      (item): item is AgentQATestPlanCase =>
+        typeof item === "object" &&
+        item !== null &&
+        typeof item.id === "string" &&
+        typeof item.name === "string" &&
+        typeof item.method === "string" &&
+        typeof item.path_template === "string" &&
+        Array.isArray(item.expected_status_codes) &&
+        item.expected_status_codes.every(
+          (status: unknown) => typeof status === "number",
+        ) &&
+        typeof item.rule === "string",
+    );
+
+    return {
+      id: plan.id,
+      api_title: plan.api_title,
+      base_url: plan.base_url,
+      cases,
+    };
+  } catch {
+    return null;
+  }
+}
+
+export function extractPresentFilesFromMessage(
+  message: Message,
+  results: Message[] = [],
+) {
   if (message.type !== "ai" || !hasPresentFiles(message)) {
     return [];
   }
@@ -451,9 +541,43 @@ export function extractPresentFilesFromMessage(message: Message) {
       Array.isArray(toolCall.args.filepaths)
     ) {
       files.push(...(toolCall.args.filepaths as string[]));
+    } else if (
+      ["agentqa_execute_test_plan", "agentqa_compare_test_reports"].includes(
+        toolCall.name,
+      )
+    ) {
+      const result = results.find(
+        (candidate) =>
+          candidate.type === "tool" &&
+          !!toolCall.id &&
+          candidate.tool_call_id === toolCall.id,
+      );
+      if (!result) continue;
+      try {
+        const payload = JSON.parse(extractTextFromMessage(result)) as {
+          status?: unknown;
+          artifacts?: unknown;
+        };
+        if (
+          payload.status === "completed" &&
+          Array.isArray(payload.artifacts)
+        ) {
+          files.push(
+            ...payload.artifacts.filter(
+              (path): path is string =>
+                typeof path === "string" &&
+                path.startsWith("/mnt/user-data/outputs/") &&
+                !path.includes("\\") &&
+                !path.split("/").includes(".."),
+            ),
+          );
+        }
+      } catch {
+        // Incomplete/failed tool results must not advertise nonexistent reports.
+      }
     }
   }
-  return files;
+  return [...new Set(files)];
 }
 
 export function hasSubagent(message: AIMessage) {
diff --git a/scripts/docker.sh b/scripts/docker.sh
index 8123d42..ddfd9f2 100755
--- a/scripts/docker.sh
+++ b/scripts/docker.sh
@@ -191,9 +191,9 @@ start() {
 
     sandbox_mode="$(detect_sandbox_mode)"
 
-    services="frontend gateway nginx"
+    services="frontend gateway agentqa-demo-api nginx"
     if [ "$sandbox_mode" = "provisioner" ]; then
-        services="frontend gateway provisioner nginx"
+        services="frontend gateway agentqa-demo-api provisioner nginx"
     fi
 
     # Only aio mode (AioSandboxProvider without provisioner_url) needs the host
@@ -271,6 +271,7 @@ start() {
     echo ""
     echo "  🌐 Application: http://localhost:2026"
     echo "  📡 API Gateway: http://localhost:2026/api/*"
+    echo "  🧪 AgentQA Demo: http://localhost:8003/docs"
     echo "  🤖 Runtime:     Gateway embedded"
     echo "  API:            /api/langgraph/* → Gateway"
     echo ""
